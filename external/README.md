# External ZDPlasKin environment

The ZDPlasKin executable/runtime, compiler, BOLSIG files, and controlled
mechanism sources are external to this repository.  Configure their local paths
outside the source tree and record the resolved paths and file hashes in each
local case manifest.  Never commit generated DLLs, build products, runtime
logs, or machine-specific absolute paths.
