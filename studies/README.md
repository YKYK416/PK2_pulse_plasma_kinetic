# Studies

A study groups many related case configurations and records which plot recipes
and validation criteria apply to them.  A study file must point to configurations
or manifest locations, never require a manually remembered output path.

`gas_phase_time_scan/` indexes the accepted legacy closed-0D time horizons.  It
does not convert those existing data files into a new format.
